<%= render "ni_mappings_header" %>

<table class="table">
<thead>
  <tr>
    <th>#</th>
    <th><%= sortable("name", "Register Office Name") %></th>
    <th><%= sortable("council", "Council") %></th>
  </tr>
</thead>

<% @mappings.each_with_index do |mapping, index| %>
<tr>
  <td><%= index + 1 %></td>
  <td><%= mapping.name %></td>
  <td><%= mapping.council %></td>
</tr>
<% end %>
</table>
